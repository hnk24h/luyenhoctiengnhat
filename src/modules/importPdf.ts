// Import nội dung từ PDF text-based (placeholder)
// Có thể dùng pdf-parse hoặc pdfjs-dist để extract text

export async function importContentFromPdf(filePath: string): Promise<string> {
  // TODO: Triển khai extract text từ PDF
  // Ví dụ: dùng pdf-parse (Node.js)
  // const data = await pdfParse(fs.readFileSync(filePath));
  // return data.text;
  return 'Chức năng import PDF sẽ được phát triển sau.';
}
